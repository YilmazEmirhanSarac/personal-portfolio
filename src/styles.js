// Shared Tailwind class strings, so UI details stay consistent across components.

// Visible keyboard focus outline — browsers only show it for keyboard users.
export const focusRing =
  'rounded-sm focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-neutral-900'
